Git setup
--------------
ssh-keygen -t ed25519 -C "sbouhet@gmail.com"
cat ~/.ssh/id_ed25519.pub
=> copy it in https://github.com/settings/keys (ssh keys)
git config --global user.name "sbouhet"
git config --global user.email "sbouhet@gmail.com"

Git repo
--------------
git clone git@github.com:sbouhet/apsystems_alerting.git
git add .gitignore
git commit -m 'first commit'
git push -u origin main

